# 컬러 패턴 생성 고도화 보고서

대상 구간: `a95cc28` 이후부터 `98044f3`(HEAD)까지.

## 요약

컬러 패턴 품질을 결정한 것은 프롬프트 문장이 아니라 **모델에 넣는 이미지 조건**이었다.
같은 결함을 산문 지시로 고치려던 시도는 세 번 모두 실패했고, 같은 결함을 이미지 쪽에서
고친 시도는 세 번 모두 성공했다. 현재 채택된 설계는 네 가지다.

1. `inputs/photos/<신발>/` 뷰별 파일 계약과 한국어 뷰 라벨
2. Step 1 명세서를 pydantic 스키마로 강제
3. Step 2에 lateral·medial 사진을 직접 전달하고, 가이드라인은 **비대칭 외곽 + 여백 라벨** 판본을 사용
4. 출력은 4K·2:3 세로 고정

폐기된 접근은 자유 종횡비, 가이드라인 내부 라벨, 필수 스키마 필드 추가(`평면형태`),
산문으로 지시하는 앞코 절개, 폴더 내용 추측 기반 입력 해석이다.

---

## 1. 범위와 판단 기준

이 보고서는 위 커밋 구간에서 **컬러 패턴 생성 경로**(현재 `services/color_pattern/`,
당시 `config/prompts.py`·`core/`)에 직접 영향을 준 변경만 다룬다. 스케치 패턴 전용 변경은
컬러 패턴에 파급된 경우에만 언급한다.

증거의 등급을 두 단계로 구분한다. 이 구분이 이후 판단의 신뢰도를 좌우한다.

- **A급 — 통제된 실험**: Step 1 명세서를 고정(diff=0)하고 조건당 2회 이상, 여러 신발,
  무회귀 대조군 포함. 해당: `64d69ef`, `f6f9d3d`, `6f267ba`, `955675b` 일부.
- **B급 — 단발 관측**: 조건당 1회, 신발 1켤레, 대조군 없음. 해당: 2026-08-04
  09:40~11:02 사이의 Step 2 사진 재주입 실험 전체.

---

## 2. 시도한 방법론과 결과 (시간순)

### 2.1 멀티뷰 입력과 관찰 스텝 분리 — 채택

`134aac2`(뷰 플래그 6개), `a8b43d7`(이미지마다 각도 라벨 전송), `046097a`(관찰 스텝 추가와
규칙 우선순위 도입), `921eea3`(스텝별 응답 모달리티), `7e1eb48`(front/heel을 쿼터 뷰로 정정).

한 장에서 모든 것을 읽던 방식을 버리고, 뷰별 사진에 각도 라벨을 붙여 보낸 뒤
관찰(Step 1)과 펼침(Step 2)을 분리했다. 이후 모든 규칙이 이 라벨 문자열을 그대로 지칭하기
때문에, 지금도 `services/color_pattern/photo_input.py:VIEW_LABELS`의 문자열은 바꾸면
프롬프트 규칙이 함께 깨진다. 코드에 그 취지가 주석으로 남아 있다.

### 2.2 Step 2에 사진을 직접 재주입 — 두 번 실패, 세 번째에 채택

**1차 (`c6af49e`~`5b41e70`)**: 설계 문서와 계획서를 쓰고 참조 조립기(`core/_parts_builder.py`)까지
구현했으나, `28afe0a`에서 설계·계획 문서를 포함해 1222줄을 통째로 롤백했다. 롤백 커밋 자체에는
정량 근거가 남아 있지 않다.

**2차 (`0cb22be` → `dae35ac` → `5972720` → `8ecde4b`)**: 2026-08-04 09:51부터 10:19까지
약 9분 간격으로 조건을 바꿔가며 재시도했다. lateral만 주면 뒤축 절개가 사라지고,
front·heel까지 더하면 나비 모양 레이아웃이 나오고, lateral+top으로 줄여도 히스토리만 쓰던
방식보다 나빴다는 관측을 `6079d2c`에 기록하고 되돌렸다.

**이 관측은 교란되어 있다.** 조건당 1회였고, 문제로 지목된 가로 레이아웃 증상은 8분 뒤
`7f2e1f2`가 종횡비를 2:3으로 고정하면서 별도로 해소됐다. 즉 당시 실험은 프롬프트 효과와
자유 종횡비로 인한 노이즈를 구분할 수 없었다.

**3차 (`9d6c5f2`, `955675b`)**: 명세서 기준면을 lateral로 못박은 뒤 lateral을 다시 넣고,
이어서 medial을 추가했다. `955675b`은 kayano 14에서 좌우 로고 중복이 사라졌다고 기록한다.
현재 `UNFOLD_VIEWS = ("lateral", "medial")`로 남아 있다. 이 실험 당시에는 front·heel·top이
채팅 히스토리로 Step 2에 닿았지만, 이후 세션이 스텝별로 분리되면서 그 경로는 없어졌다.
지금 Step 2가 받는 것은 lateral·medial 사진, 가이드라인, Step 1 명세서, 프롬프트뿐이다.

### 2.3 출력 종횡비 자동 선택 → 2:3 고정 — 채택

`7f2e1f2`. 자동 선택이 가로 캔버스를 내놓아 앞코-아래/뒤축-위 방향 규칙이 깨졌다.
현재 `config/gemini.py:IMAGE_CONFIG`는 4K·2:3이며, 이는 미관 설정이 아니라 `fit_rule`이
성립하기 위한 전제다.

### 2.4 자유 산문 명세서 → pydantic 스키마 강제 — 채택

`955675b`. 프롬프트로 부탁하던 `경계` 필드가 실측 11개 부품 **전부**에서 누락됐고,
대칭성 어휘도 규정 밖 표현이 계속 나왔다. 스키마가 둘 다 기계적으로 막는다.
같은 커밋에서 Step 2가 명시적 이미지만 받도록 바꿔, 폴더 탐색이 신발 사진을 펼침 틀로
보내던 경로를 제거했다. liteblaze 삼선이 3/3으로 바깥쪽에만 나왔다.

다만 검증 전용이다. `step_1_part_survey.run()`은 원문 텍스트를 그대로 반환한다.
재직렬화하면 Step 2가 보는 토큰이 달라지기 때문이다.

### 2.5 필수 스키마 필드 `평면형태` 추가 — 6시간 만에 되돌림

`e376da5`(02:04) → `b685d6e`(08:23). eyestay가 vamp까지 내려온 것만 보고 개선으로 판단했으나,
실물과 대조하니 liteblaze의 lateral·medial 패턴 전체가 이전 판본보다 나빠졌다.
**필수 필드를 만들자 모델이 그 칸을 채우려고 부품을 좌우로 쪼개 읽어 부품 수가 7개에서
13개가 됐고, 그 과분할이 충실도를 떨어뜨렸다.**

같은 실패가 앞서 `경계`를 필수로 만들었을 때도 있었다(오버레이를 lateral/medial 쌍으로
쪼개는 데 출력을 다 쓰고 스우시를 잃음). 이 구간에서 가장 이전 가능한 교훈이다.

### 2.6 가이드라인 라벨 위치 — 내부 라벨 폐기, 여백 라벨 채택

`955675b`이 만든 내부 라벨 판본이 SONOMA에서 아식스 교차 스트라이프를 세로 리브 다발로
뭉개고 외곽을 달걀처럼 둥글게 만들었다. `64d69ef`가 명세서를 고정한 단일 변수 실험으로
원인을 라벨 **위치**로 특정했다(각 조건 2회).

| 조건 | 결과 |
|---|---|
| 내부 라벨 | 퇴행 2/2 |
| 라벨 없음 | 복원 2/2 |
| 여백 라벨 | 복원 2/2 |

라벨의 존재가 아니라 위치가 원인이었다. 글자가 패턴 안에 있으면 모델이 그 이미지를
재단 틀이 아니라 주석 달린 도해로 읽는다. 결정적으로, **"외곽 형태를 바꾸지 마",
"달걀처럼 둥글어지면 실패"라는 프롬프트 문장을 이미 넣어둔 상태에서 나온 퇴행이었다.**
`scripts/mark_guideline_margin.py`가 캔버스만 넓혀 여백에 라벨을 놓는다.

### 2.7 가이드라인 외곽을 비대칭으로 변형 — 채택

`f6f9d3d`. 한쪽에만 있는 로고가 양쪽에 거울상으로 복제되는 결함의 원인은 정보 부족이
아니었다. medial 사진에 그 로고가 없고 명세서도 "한쪽만"이라고 정확히 적었는데도
Step 2가 양쪽에 그렸다. 원인은 가이드라인 외곽이 좌우 대칭(미러 IoU 0.94)이라는 점이었다.
규칙 5순위가 "바깥 재단선을 가이드라인 형태에 맞춘다"이므로, **대칭 이미지 조건이
"두 반쪽은 서로 다른 면"이라는 텍스트 조건을 이겼다.**

| 신발 | 변경 전 → 후 |
|---|---|
| AM95 빨간 스우시 | 양쪽 → 왼쪽만 |
| KAYANO 워드마크 | 양쪽 → 왼쪽만 |
| liteblaze 삼선 | 양쪽 → 왼쪽만 |
| GEL-NYC 좌우 상이 로고 | 뭉개짐 → asics/GEL로 갈림 |

대조군인 SONOMA와 Vomero 5는 그대로 대칭으로 나와 회귀가 없었다. 진폭은 미러 IoU로 골랐다.
0.075는 편향을 깨지 못하고 0.16은 라스트 형태에서 멀어져 0.13(IoU 0.86)을 채택했다.

### 2.8 앞코 절개 — 산문 지시(v23) 폐기, 이미지 유도(v24) 채택

`6f267ba`. `toe_rule` 10줄로 절개를 직접 지시한 판본은 절개가 vamp·eyestay까지 관통해
패턴이 두 조각으로 갈라졌고, 3개 모델 × 2회에서 재현됐다. 그 블록을 지우고 허용 문구와
가이드라인의 V자 다트만 남긴 판본은 같은 3개 모델 × 2회에서 절개를 유지하면서 중간이
이어졌다. **강한 산문 지시가 오히려 절개 범위를 잃게 만든다**는 결론으로 지시하지 않는
쪽을 택했다.

### 2.9 입력 해석: 폴더 내용 추측 폐기 — 채택

`b7305ea`. 폴더 내용으로 모드를 추측하던 로직이 Step 1을 라벨 없는 한 장으로 퇴화시키고
Step 2의 참조 사진까지 조용히 날리고 있었다. 지금은 `inputs/photos/<신발>/` 구조를
요구하고, 없으면 **유료 API 호출 전에** 실패한다. 같은 취지로 `services/color_pattern/service.py`가
Step 2에서야 열리는 가이드라인 파일을 시작 시점에 미리 검증한다.

### 2.10 서비스 단위 재구조화 — 채택 (동작 보존)

`2f0b772`(PRD·계획) → `9a1e62a`(골든 요청 캡처) → `b7305ea`·`ed5bde2`·`39f4f09`·`d15856a` →
`fc88952`(옛 구조 삭제). 재구조화 전에 API 요청을 골든 픽스처로 먼저 캡처하고, 이후 모든
작업에서 바이트 동일성을 유지했다. 세션 경계를 서비스 경계와 일치시킨 구조 변경이며
생성 품질에 대한 변경이 아니다.

### 2.11 이미지 모델 교체 — 채택했으나 컬러 패턴 근거 없음

`545de83`이 `gemini-3.1-flash-image`로 바꾸고 thinking level을 high로 올렸다. 커밋 메시지는
한 줄이며 스케치 패턴 맥락으로 쓰였지만, 변경은 전역이었다. 현재
`services/color_pattern/service.py:MODEL`이 이 모델이다. **2.4~2.8의 실험은 모두 이전 모델에서
측정된 것이고, 교체 이후 컬러 패턴 규칙은 재검증되지 않았다.**

---

## 3. 최종 채택안

| 층 | 채택된 것 | 근거 커밋 |
|---|---|---|
| 입력 계약 | `inputs/photos/<신발>/{lateral,medial,front,heel,top,bottom}` 고정, 뷰 라벨 문자열 불변 | `a8b43d7`, `b7305ea` |
| Step 1 | 관찰 전용 스텝 + pydantic `Survey` 스키마 검증, 원문 텍스트 그대로 전달 | `046097a`, `955675b` |
| Step 2 입력 | lateral·medial 사진, 가이드라인, Step 1 명세서, 프롬프트만 직접 전달 (세션 분리로 나머지 뷰는 더 이상 닿지 않음) | `9d6c5f2`, `955675b` |
| 가이드라인 | 비대칭 외곽(AMP 0.13) + 여백 라벨 판본 | `64d69ef`, `f6f9d3d` |
| 절개 유도 | 산문 지시 없이 가이드라인 V자 다트로 유도 | `6f267ba` |
| 출력 | 4K, 2:3 세로 고정, temperature 0 | `7f2e1f2` |
| 세션 | 스텝별로 세션 2개(Step 1: `gemini-3.6-flash`, Step 2: `gemini-3.1-flash-image`) — 모델이 세션 생성 시 고정되므로 스텝마다 다른 모델을 쓰려면 세션도 나눠야 함 | `1b4b154`, `ed5bde2` |

관통하는 원칙 하나: **이미지 조건과 텍스트 조건이 충돌하면 이미지 조건이 이긴다.**
라벨 위치(2.6), 가이드라인 대칭성(2.7), 앞코 절개(2.8) 세 사례가 각각 독립적으로 이를
보여줬고, 셋 모두 산문 대응이 먼저 실패한 뒤였다.

---

## 4. 잔여 한계

1. **현재 모델에서의 재검증 부재.** 규칙 실험은 모두 이전 이미지 모델에서 측정됐다.
   삼선이 바깥쪽에만 나오는지, 뒤축 절개가 남는지, 앞코 절개가 eyestay까지 가지 않는지는
   지금 모델에서 다시 확인해야 한다.
2. **B급 증거의 오독 위험.** `6079d2c`의 "Step 2 사진 재주입은 역효과" 결론은 조건당 1회이며
   자유 종횡비 아래에서 측정됐다. 현재 코드는 이미 이 결론과 반대로 동작하므로 깨진 것은
   없지만, 이 커밋을 근거로 다시 판단하면 안 된다.
3. **실험 로그의 부재.** 측정 하네스(`scripts/run_v6_parallel.sh`, `sketch_scoring/`)는
   만들어졌다가 삭제됐다(`697cc6d`). 모든 수치는 커밋 메시지에만 남아 있다.
4. **미해결 문제로 명시된 것.** `b685d6e`이 남긴 eyestay 평면 형상 누락은 여전히 열려 있다.
   같은 커밋이 다음 시도 방향까지 지정해 뒀다: 필수 필드 추가가 아니라 **Step 2에서만 top
   사진을 참조**하게 하는 쪽. 이 구간에서 유일하게 설계까지 끝나고 시도되지 않은 실험이다.
   이후 세션이 스텝별로 분리되면서 top은 지금 어떤 경로로도 Step 2에 닿지 않으므로,
   이 실험은 여전히 유효하며 오히려 더 직접적으로 필요해졌다.

## 5. 다음 실험 제안

- 현재 모델 기준 회귀 확인: 신발 3켤레 × 2회, 명세서 고정, 위 1번의 네 항목만 채점.
- `UNFOLD_VIEWS`에 `top` 추가 A/B: `b685d6e`이 지정한 방향. 명세서 고정, 조건당 2회.
- 스키마 확장은 필수 필드가 아닌 선택 필드로만. 필수화는 부품 판독 자체를 바꾼다.
